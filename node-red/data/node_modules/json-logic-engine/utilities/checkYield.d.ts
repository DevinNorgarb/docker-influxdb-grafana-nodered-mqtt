export default checkYield;
/**
 * Checks if the item that's passed in is or contains a Yield / EngineObject (signifying a Yield)
 * @param {*} item
 * @returns {Boolean}
 */
declare function checkYield(item: any): boolean;
